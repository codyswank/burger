
<div class="row">
    <div class="col-12 ">
            <h1 class="burger">Eat-Da-Burger!</h1>
    </div>
</div>
<br>
<div class="row">
    <div class="col-3"></div>
<div class="col-3 toEat rounded">
    <ul>
        {{#each burgers}} {{#unless this.devoured}}
        <li class="rounded">
            <div class="row">
                <div class="col-8">
            {{this.id}}. {{this.burger_name}}
                </div>
                <div class="col-4">
            <form action="/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="false">
                <button class="btn btn-warning" type="submit">Eat Me!</button>
            </form>
            </div>
            </div>

        </li>
        {{/unless}} {{/each}}
    </ul>
</div>

<div id='col-3'>

    <ul>
        {{#each burgers}} {{#if this.devoured}}
        <li class="rounded">
            {{this.id}}. {{this.burger_name}}
        </li>
        {{/if}} {{/each}}
    </ul>
</div>
<div class="col-3"></div>
</div>
<div class="row">
    <div class="col-4"></div>
    <div class="col-4  cent">
        <div class="row">
            <div class="col-12">
                 <h2>Add a Burger: </h2>
                    <form class="create-update-form" action="/" method="POST">
        <div class="form-group">
            <input type="text" id="dab" name="burger_name">
        </div>
            </div>
        </div>
   

    
        <button class="btn btn-primary" type="submit">Add Burger</button>
    </form>
    </div>
    <div class="col-4"></div>
</div>

